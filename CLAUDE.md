# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repo is currently a blank slate: only `README.md` (a single heading), `LICENSE` (Apache 2.0), and a Python-oriented `.gitignore` exist. There is no source code, package manifest, build config, or test suite yet. The project name is "Handwritten" but its scope has not been written down anywhere in-repo.

When the user asks for the first piece of real work, the choice of language, framework, and layout is still open. Confirm direction with the user before scaffolding — don't assume Python just because `.gitignore` is Python-flavored (it may have been generated by default).

## Once code lands

When real source/build/test infrastructure is added, update this file with:
- the build / lint / test commands actually used (including how to run a single test)
- the high-level architecture that spans multiple files
- any non-obvious conventions the user establishes

Until then, there is nothing project-specific to document beyond this note.
